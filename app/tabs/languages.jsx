import React from "react";
import Container from "react-bootstrap/Container";

export const Languages = () => (
  <Container>
    Abble to communicate Russian (native) and English (upper intermediate)
  </Container>
);
